# MCP / Tool Gateway

The hackathon build includes a provider-agnostic MCP-style pathway.

Demo endpoint:

```text
POST /api/mcp/gateway/test
```

Mock tools are under:

```text
services/mock-tools/
```

These simulate ticket retrieval, policy context, and compliance blockers. The goal is to prove tool evidence and recovery without depending on private APIs.
