[project]
name = "tracememory"
version = "0.1.0"
description = "TraceMemory SDK for durable runtime memory, checkpoints, recovery, and adapters for agent frameworks."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["tracememory*"]
