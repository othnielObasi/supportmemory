# OpenClaw Plugin

The OpenClaw plugin package is included under:

```text
packages/openclaw-plugin-tracememory/
```

It is the integration path for capturing agent lifecycle events, tool calls, checkpoints, and context-health metadata from OpenClaw-powered agents.

For the hackathon demo, the UI and API use a deterministic recovery story so the project can run without external keys.
